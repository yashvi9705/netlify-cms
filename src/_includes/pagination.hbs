{{!-- 
<div class="pagination-controls">
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
            <li class="page-item">
                {{#if pagination.href.previous}}
                <a href="{{ pagination.href.previous }}" class="page-link active">Previous</a>
                {{else}}
                <a class="page-link disabled">Previous</a>
                {{/if}}
            </li>

    
            <li class="page-item">

                {{#each pagination.pages as |pageEntry index|}}
                    
                    <li class="page-item">
                    <!-- Check if it's Page 1 -->
                    {{#if (eq (inc index) 1)}}
                    <a 
                        {{#if (eq @root.pagination.pageNumber index)}}
                        class="page-link active"
                        {{else}}
                        class="page-link"
                        {{/if}}
                        href="/employeeDirectory/"> {{ inc index }}
                    </a>
                    {{else}}
                    <!-- Check for other pages -->
                    <a 
                        {{#if (eq @root.pagination.pageNumber index)}}
                        class="page-link active"
                        {{else}}
                        class="page-link"
                        {{/if}}
                        href="/employeeDirectory/{{ index }}/"> {{ inc index }}
                    </a>
                    {{/if}}
                </li>
                {{/each}}
            </li>
    
    
            <li class="page-item">
                <!-- Next Link -->
                {{#if pagination.href.next}}
                <a href="{{ pagination.href.next }}" class="page-link active"> Next</a>
                {{else}}
                    <a class="page-link disabled">Next</a>
                {{/if}}
            </li>
        </ul>
    </nav>
</div> --}}


<div class="pagination-controls">
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
            <li class="page-item">
                {{#if pagination.href.previous}}
                <a href="{{ pagination.href.previous }}" class="page-link active">Previous</a>
                {{else}}
                <a class="page-link disabled">Previous</a>
                {{/if}}
            </li>
    
    
            <li class="page-item">
                <!-- Next Link -->
                {{#if pagination.href.next}}
                <a href="{{ pagination.href.next }}" class="page-link active"> Next</a>
                {{else}}
                    <a class="page-link disabled">Next</a>
                {{/if}}
            </li>
        </ul>
    </nav>
</div>